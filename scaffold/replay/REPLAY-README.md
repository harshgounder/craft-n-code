# Offline replay bundle (wifi-death story)

Screenshots are at docs/screenshots/ (the team takes them during rehearsal, this build never generates them).

Run `./replay.sh` to serve the signed `happy` fixture with zero network; it checks the sha256 signature and prints the pipeline trace IDs.

While it loads, say: "this demo runs fully offline, every pipeline step is traced, and the mode badge only reports real provider outcomes."
